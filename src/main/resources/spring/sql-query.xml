<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

    <bean id="getLimitNumberOfAdverts" class="java.lang.String">
        <constructor-arg value="SELECT a.id, a.title, a.text, a.modificationDate, a.price,
                   a.currency, a.status FROM advert as a LIMIT :startPosition, :maxAdvertsPerPage"/>
    </bean>

    <bean id="getAllAdverts" class="java.lang.String">
        <constructor-arg value="SELECT a.id, a.title, a.text, a.modificationDate, a.price,
                   a.currency, a.status FROM advert as a;"/>
    </bean>
    <bean id="getAllCategories" class="java.lang.String">
        <constructor-arg value="SELECT c.id, c.name, c.parentId FROM category as c;"/>
    </bean>
    <bean id="getAllUsers" class="java.lang.String">
        <constructor-arg value="SELECT u.id, u.name, u.email, u.password, u.phone, u.avatar,
        u.status, u.type, u.dislikeAmount FROM user as u;"/>
    </bean>
    <bean id="getAllImages" class="java.lang.String">
        <constructor-arg value="SELECT a.id, a.picture, a.type FROM advertPicture as a;"/>
    </bean>
</beans>